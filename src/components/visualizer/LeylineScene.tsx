
import React from 'react';
import { SceneProps } from './SceneProps';

const LeylineScene: React.FC<SceneProps> = ({ analyzer, isPlaying }) => {
  return <div className="leyline-scene">Leyline Scene</div>;
};

export default LeylineScene;
