
import React from 'react';
import { SceneProps } from './SceneProps';

const NebulaScene: React.FC<SceneProps> = ({ analyzer, isPlaying }) => {
  return <div className="nebula-scene">Nebula Scene</div>;
};

export default NebulaScene;
