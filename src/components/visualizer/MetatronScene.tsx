
import React from 'react';
import { SceneProps } from './SceneProps';

const MetatronScene: React.FC<SceneProps> = ({ analyzer, isPlaying }) => {
  return <div className="metatron-scene">Metatron Scene</div>;
};

export default MetatronScene;
