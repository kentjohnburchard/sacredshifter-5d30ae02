
import React from 'react';
import { SceneProps } from './SceneProps';

const FractalScene: React.FC<SceneProps> = ({ analyzer, isPlaying }) => {
  return <div className="fractal-scene">Fractal Scene</div>;
};

export default FractalScene;
