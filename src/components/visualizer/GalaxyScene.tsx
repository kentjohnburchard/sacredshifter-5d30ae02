
import React from 'react';
import { SceneProps } from './SceneProps';

const GalaxyScene: React.FC<SceneProps> = ({ analyzer, isPlaying }) => {
  return <div className="galaxy-scene">Galaxy Scene</div>;
};

export default GalaxyScene;
