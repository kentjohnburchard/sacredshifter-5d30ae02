
import React from 'react';
import { SceneProps } from './SceneProps';

const HologramScene: React.FC<SceneProps> = ({ analyzer, isPlaying }) => {
  return <div className="hologram-scene">Hologram Scene</div>;
};

export default HologramScene;
